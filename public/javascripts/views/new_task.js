// Generated by CoffeeScript 1.7.1
(function() {
  define(["jquery", "underscore", "backbone", "vent", "validatable", "text!templates/tasks/new_task.html"], function($, _, Backbone, Vent, Validatable, newTaskTemplate) {
    var newTaskView;
    newTaskView = Backbone.View.extend({
      initialize: function() {
        this.listenTo(this.model, 'invalid', this.renderErrors);
        this.listenTo(this.model, "error", this.parseErrorResponse);
        this.listenTo(this.model, 'sync', this.render);
        if (!this.model.isNew()) {
          return this.model.fetch();
        }
      },
      events: {
        'submit': 'saveProject'
      },
      render: function() {
        var compiledTemplate;
        compiledTemplate = _.template(newTaskTemplate);
        this.$el.html(compiledTemplate);
        return this;
      },
      saveProject: function(e) {
        e.preventDefault();
        this.model.set({
          name: this.$("#name").val()
        });
        if (this.$("#comment").val() === '') {
          this.model.set({
            comments: []
          });
        } else {
          this.model.set({
            comments: [
              {
                comment: this.$("#comment").val(),
                by: 'John Doe',
                created_at: new Date()
              }
            ]
          });
        }
        this.model.set({
          importance: this.$("#importance").val()
        });
        return this.model.save({
          wait: true
        }, {
          success: function(model, data) {
            return Vent.trigger("task:create", model);
          }
        });
      }
    });
    _.extend(newTaskView.prototype, Validatable);
    return newTaskView;
  });

}).call(this);
